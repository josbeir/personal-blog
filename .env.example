# Optional, but recommended for production builds: GitHub's unauthenticated
# API limit is low and shared by IP address. Create a fine-grained token with
# read-only access to public repositories and set it in the hosting provider.
GITHUB_TOKEN=
